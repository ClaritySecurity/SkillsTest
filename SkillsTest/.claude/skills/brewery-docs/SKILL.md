---
description: Use when the user asks about the Open Brewery DB API -- endpoints, schema, pagination, filtering, brewery types, or query parameters. Read the local docs instead of relying on training data.
allowed-tools: Read, Glob
---

Read the Open Brewery DB API documentation from `docs/brewery-api/` in this project.

Based on what the user is asking about, find and read the most relevant doc file(s):
- Endpoints, query params, filtering, search -> `docs/brewery-api/endpoints.md`
- Brewery fields, types, schema -> `docs/brewery-api/schema.md`
- Pagination, per_page, page, total count -> `docs/brewery-api/pagination.md`

If unclear which file is relevant, read all three -- they're short.

Return the documentation content directly -- don't summarize or interpret it.
